/**********************************************************IDEO - Adam GlazierCopyright 2011No Rights Reserved NOTICE:  Don't worry about using or abusing this code *********************************************************//********************************************************** MoveItemsToNearestPixel.jsxDESCRIPTIONMoves any selected objects to the nearest pixel. **********************************************************/if ( documents.length > 0 ) {		var sourceDoc = activeDocument;	var items = selection;	var textRefs = sourceDoc.textFrames;	var pathRefs = sourceDoc.pathItems;	var symRefs = sourceDoc.symbolItems;	var i = 0 ;			if ( items.length == 0 ) {				alert( "You idiot! Select at least one item before running this script.");			} else {				// move items to nearest pixel		for ( i = 0 ; i < items.length; i++ ) {						items[i].left = Math.round( items[i].left );			items[i].top = Math.round( items[i].top );					}				/*		// zero out text		for ( i = 0 ; i < textRefs.length; i++ ) {						textRefs[i].left = Math.round( textRefs[i].left );			textRefs[i].top = Math.round( textRefs[i].top );					}						// zero out paths		for ( i = 0 ; i < pathRefs.length; i++ ) {						pathRefs[i].left = Math.round( pathRefs[i].left );			pathRefs[i].top = Math.round( pathRefs[i].top );					}						// zero out symbols		for ( i = 0 ; i < symRefs.length; i++ ) {						symRefs[i].left = Math.round( symRefs[i].left );			symRefs[i].top = Math.round( symRefs[i].top );					}		*/				redraw();			}	} else {		alert( "Dummy head! Create a document with something in it before running this script." );	}