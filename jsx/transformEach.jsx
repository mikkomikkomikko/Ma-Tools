var doc = app.activeDocument;  var doc_selection = doc.selection;  var groupSelection;var scaleDir;function selections() {    if (groupSelection.value == true) {         $.writeln ("true");         }   else {        $.writeln ("false");        }}function objScale(){    var doc = app.activeDocument;      var doc_selection = doc.selection;      //$.writeln (scaleDir);       var posScale = parseInt(factorText.text) + 100;    var negScale = 100 - parseInt(factorText.text);    var endIndex = doc_selection.length;        for ( i = 0; i < endIndex; i++ ) {        if (scaleDir) {            $.writeln (posScale);              doc_selection[i].resize( posScale, posScale ); // scaleAbout        } else {            $.writeln (negScale);            doc_selection[i].resize( negScale, negScale ); // scaleAbout        }    }}objScale ();